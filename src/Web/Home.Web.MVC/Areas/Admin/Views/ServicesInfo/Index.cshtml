@model List<Home.Web.Areas.Admin.Models.ServicesInfo.Service>

@{ 
    Layout = "_Layout";
    ViewData["Title"] = "Observed Services";
}

<div>
    @foreach (var service in Model)
    {
        <ol>@service.Name</ol>
    }
</div>